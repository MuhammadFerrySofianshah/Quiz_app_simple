name: quiz_app_simple
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  connectivity: ^3.0.6
  cupertino_icons: ^1.0.2
  loading_animation_widget: ^1.2.1
  countdown_progress_indicator: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/images/